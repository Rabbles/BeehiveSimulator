using BeehiveSimulator.Model;
using NUnit.Framework;
using System.Drawing;

namespace BeeSimulatorTest
{
    [TestFixture]
    public class BeeTest
    {
        [SetUp]
        private void Setup()
        {
            //var bee = new Bee(1, new Point(50, 50), new);
        }

        private void 


        [TearDown]
        private void TearDown()
        { 
        }
    }
}
